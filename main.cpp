#include <math.h>
#include "change conversion.h"
#include "coin count.h"

int main()
{
	//coin_count returns the number of coins that will be used to provide change
	cout << " You will need to use " << coin_count() << " coins";
}